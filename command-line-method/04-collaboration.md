
# 04 — Collaboration (Command Line)

Clone:
```bash
git clone git@github.com:org/protocols.git
```
Fork model:
```bash
git remote add upstream git@github.com:original/protocols.git
git fetch upstream
git merge upstream/main
git push origin main
```
PR flow: branch → push → open PR on GitHub → review → merge.
---


